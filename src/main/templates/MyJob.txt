package ${packageName}

import groovy.transform.CompileStatic

@CompileStatic
class ${className}Job {
    static triggers = {
      simple repeatInterval: 5000l // execute job once in 5 seconds
    }

    def execute() {
        // execute job
    }
}
